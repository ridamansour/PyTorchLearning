#!/bin/sh

echo "Running progress report update before push..."

# Run your Python script
python3 -c "from Progress.course_progress_func import progress_report_to_README; progress_report_to_README()"

# Add and commit the updated README automatically
git add README.md
git commit -m "Auto-update README with progress report" --no-verify || true
